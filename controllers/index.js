/**
 * Controllers index
 */

const main = require('./main')
const user = require('./user')
const pet = require('./pet')
const user_pet = require('./user_pet')

module.exports = { main, user, pet, user_pet }
